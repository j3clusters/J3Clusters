# Static HTML prototype

Self-contained mockup (HTML, `styles/`, `scripts/`). Open `index.html` in a browser from this folder, or serve the folder with any static file server.

The production app lives in `src/` (Next.js).
