import type { Listing } from "@/types/listing";

import { stepsstoneListings } from "./stepsstone-listings.generated";

/** Published listings — imported from StepsStone project data */
export const listings: Listing[] = stepsstoneListings;
